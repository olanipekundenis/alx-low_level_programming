#include "lists.h"
 
/** 
 * insert_dnodeint_at_index - inserts a new node where needed
 * @h: refers to head of list
 * @indx: index of the list where the new node is added
 * @n: data of the new node
 * Return: returns the address of the new node, or NULL if it failed
 */
 
dlistint_t *insert_dnodeint_at_index(dlistint_t **h, unsigned int indx, int n)
{
}
